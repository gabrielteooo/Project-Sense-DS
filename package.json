{
  "name": "project-sense-ds",
  "private": true,
  "type": "module",
  "scripts": {
    "tokens:build": "node scripts/tokens-build.mjs",
    "handbook:dev": "npm run dev --prefix handbook",
    "handbook:build": "npm run build --prefix handbook"
  }
}
